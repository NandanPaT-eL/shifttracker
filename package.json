{
  "name": "shift-tracker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Shift tracking app with calendar, hours/pay analytics, and a MongoDB Atlas backend.",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "start": "npm install --silent && npm run dev",
    "dev": "concurrently -k -n API,WEB -c blue,magenta \"npm run start --workspace backend\" \"npm run dev --workspace frontend\""
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongoose": "^8.5.1"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}

